const { mergeJestConfigWithBase } = require('manage-react-app/test');

const projectJestConfig = {
  /**
   * You can specify custom project-specific
   * Jest config here.
   */
};

module.exports = mergeJestConfigWithBase(projectJestConfig);
